<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lavanderia_cliente">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_lavanderia"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_lavanderia_round"
        android:name=".LavanderiaApplication"
        android:supportsRtl="true"
        android:theme="@style/Theme.LavanderiaCliente"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.activity.SplashScreen"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.MainActivity" />
    </application>

</manifest><!--        android:name=".LavanderiaApplication"-->
